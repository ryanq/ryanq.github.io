version: 2.1

jobs:
  install:
    docker:
      - image: cimg/base:node@17.0.1
    steps:
      - checkout
      - run: yarn
  build:
    docker:
      - image: cimg/base:node@17.0.1
    steps:
      - run: yarn build

workflows:
  build:
    jobs:
      - install
      - build
          requires:
            - install
