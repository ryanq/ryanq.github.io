version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:17.0.1
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn
      - run:
          name: Build site
          command: yarn build

workflows:
  build:
    jobs:
      - build
